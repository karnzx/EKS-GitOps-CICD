apiVersion: v1
kind: Service
metadata:
{{- if .Values.backend.redisMaster.service.annotations }}
  annotations:
  {{- range $key, $value := .Values.backend.redisMaster.service.annotations }}
    {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
  labels:
    {{- include "guestbook.labels" (dict "context" . "component" .Values.backend.redisMaster.name "name" .Values.backend.redisMaster.name) | nindent 4 }}
{{- with .Values.backend.redisMaster.service.labels }}
{{- toYaml . | nindent 4 }}
{{- end }}
  name: {{ .Values.backend.redisMaster.name }}
  namespace: {{ .Release.Namespace | quote }}
spec:
  ports:
  - name: {{ .Values.backend.redisMaster.service.portName }}
    protocol: TCP
    port: {{ .Values.backend.redisMaster.service.port }}
    targetPort: http
  selector:
    {{- include "guestbook.selectorLabels" (dict "context" . "name" .Values.backend.redisMaster.name) | nindent 4 }}