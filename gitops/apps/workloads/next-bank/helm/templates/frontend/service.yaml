apiVersion: v1
kind: Service
metadata:
{{- if .Values.frontend.service.annotations }}
  annotations:
  {{- range $key, $value := .Values.frontend.service.annotations }}
    {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
  labels:
    {{- include "guestbook.labels" (dict "context" . "component" .Values.frontend.name "name" .Values.frontend.name) | nindent 4 }}
{{- with .Values.frontend.service.labels }}
{{- toYaml . | nindent 4 }}
{{- end }}
  name: {{ template "guestbook.frontend.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
spec:
  ports:
  - name: {{ .Values.frontend.service.portName }}
    protocol: TCP
    port: {{ .Values.frontend.service.port }}
    targetPort: http
  selector:
    {{- include "guestbook.selectorLabels" (dict "context" . "name" .Values.frontend.name) | nindent 4 }}